// *用户 信息
// 进口商
const importer = {
  company: String,
  linkMan: String,
  tel: Number,
  address: String
};
// 出口商
const exporter = {
  company: String,
  linkMan: String,
  tel: Number,
  address: String
};

module.exports = { importer, exporter };
